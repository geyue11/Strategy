$(function () {
    // 设置样式，让内容垂直居中
    // $('.compareList>.item').each(function (i, e) {
    //     $(this).children().each(function (i) {
    //         console.log($(e).height());
    //         $(this).css('top', $(e).height() / 2 - $(this).height() / 2);
    //     });
    // });
    // $('.compareList>.item>p').each(function (i) {
    //     console.log($(this).parent().height());
    //     console.log($(this).height());
    //     $(this).css('top', $(this).parent().height() / 2 - $(this).height() / 2);
    // });
    // $('.compareList>.item').each(function (i) {
    //     $(this).children('.one').css('top', $(this).height() / 2 - $(this).children('.one').height() / 2);
    // });
    // $('.compareList>.item').each(function (i) {
    //     $(this).children('.two').css('top', $(this).height() / 2 - $(this).children('.two').height() / 2);
    // });
    // $('.compareList>.item').each(function (i) {
    //     $(this).children('.three').css('top', $(this).height() / 2 - $(this).children('.three').height() / 2);
    // });

    // checkbox 选择框
    // (function () {
    //     $('.compareList').on('click', '.item .check>span', function () {
    //         $(this).children('img').removeClass('hide').end().siblings().children('img').addClass('hide');
    //     });
    // })();
});